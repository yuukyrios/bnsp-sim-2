# CargoMoms backend API base URL.
# When running the backend locally, leave as http://localhost:5000/api.
# When deploying or using a cloud preview, set this to a public URL (e.g. ngrok, Render).
VITE_API_BASE_URL=http://localhost:5000/api